type PLAN_VALUE_CARD_PROPS = {
  Icon: React.ElementType;
  heading: string;
  subheading: string;
  iconClasses?: string;
  iconBackgroundClasses?: string;
};

export type { PLAN_VALUE_CARD_PROPS };
